{
  "routing": {
    "routes": [
      {
        "route": "/*}",
        "rewrite": "/index.html"
      }
    ]
  },
  "routes": [
    {
      "route": "/{*any}",
      "rewrite": "/index.html"
    }
  ]
}