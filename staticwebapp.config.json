{
  "routing": {
    "routes": [
      {
        "route": "/*}",
        "rewrite": "/index.html"
      }
    ]
  },
  "routes": [
    {
      "route": "/login",
      "rewrite": "/index.html",
      "statusCode": 200
    }
  ]
}