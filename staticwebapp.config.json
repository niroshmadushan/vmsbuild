{
  "routing": {
    "routes": [
      {
        "route": "/*}",
        "rewrite": "/index.html"
      }
    ]
  },
  "routes": [
    {
      "route": "/*",
      "rewrite": "/index.html",
      "statusCode": 404
    }
  ]
}